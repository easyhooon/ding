// swift-tools-version: 5.9

import PackageDescription

let package = Package(
    name: "DingRemoteSmoke",
    platforms: [
        .iOS(.v13),
    ],
    dependencies: [
        .package(
            url: "https://github.com/easyhooon/ding.git",
            exact: "__DING_VERSION__"
        ),
    ],
    targets: [
        .target(
            name: "DingRemoteSmoke",
            dependencies: [
                .product(name: "Ding", package: "ding"),
            ]
        ),
    ]
)
